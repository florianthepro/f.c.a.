```
Create a single-file **index.html** (HTML/CSS/JS) that replicates the Flipper Lab apps UI but **only** lists, compares, and manages **custom firmware apps** (e.g., RM). The page may fetch FlipperLab/stock metadata **only** to detect and filter out stock/FlipperLab apps so they never appear or are actionable. Requirements: run entirely in the user’s browser (no backend), perform **dynamic, client-side comparison and live status feedback** after loading by indexing specified GitHub repositories for fast lookups, download **only** the specific app/feature assets the user requests (from GitHub) into the browser, verify checksums and cryptographic signatures before any write, and then install/remove that custom app to a **stock‑flashed Flipper** via SD‑card copy or WebUSB/WebSerial with explicit user consent, progress/status UI, operation logs, and safety warnings. Never flash or modify device firmware; do not list or manage stock/FlipperLab apps. Output a compact, ready-to-run single-page scaffold.
